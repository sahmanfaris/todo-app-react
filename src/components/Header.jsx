import React from "react";

const Header = () => {
  return (
    <div
      style={{
        background: "#333",
        color: "#fff",
        textAlign: "center",
        padding: "10px",
      }}
    >
      <h1>TodoList</h1>
    </div>
  );
};

export default Header;
